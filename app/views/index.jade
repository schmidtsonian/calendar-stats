include _master.jade

block body

    section.wrapper
        ul.levels
            - for (var x = 0; x < 10; x++)
                li: .number= ( x + 1 ) * 10
        
        ul.bars
            - for (var x = 0; x < 12; x++)
                li: .bar a
        
        .months

        .year 2015

